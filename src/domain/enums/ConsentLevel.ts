/**
 * Niveaux de consentement disponibles.
 * Chaque niveau correspond à un degré d'intimité.
 */
export enum ConsentLevel {
  LIGHT = 'light',
  MODERATE = 'moderate',
  INTIMATE = 'intimate',
  CUSTOM = 'custom',
}
